---
layout: null
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <!-- Main/Home Page -->
  <url>
    <loc>{{ site.url }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>

  <!-- Blog Archive Page -->
  <url>
    <loc>{{ site.url }}/blog/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
  </url>

  <!-- CV PDF File -->
  <url>
    <loc>{{ site.url }}/files/woohyuk-CV-2025-09.pdf</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>

  <!-- All Blog Posts -->
  {% for post in site.posts %}
    {% if post.published != false %}
  <url>
    <loc>{{ site.url }}{{ post.url }}</loc>
    {% if post.lastmod %}
    <lastmod>{{ post.lastmod | date_to_xmlschema }}</lastmod>
    {% elsif post.date %}
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
    {% else %}
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    {% endif %}
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>
    {% endif %}
  {% endfor %}

  <!-- All Pages (excluding admin pages and the HTML sitemap) -->
  {% for page in site.pages %}
    {% if page.published != false and page.sitemap.exclude != 'yes' and page.sitemap.exclude != true %}
      {% unless page.url contains '/404' or page.url contains 'sitemap.xml' or page.url contains 'feed.xml' or page.url contains 'robots.txt' %}
  <url>
    <loc>{{ site.url }}{{ page.url }}</loc>
    {% if page.lastmod %}
    <lastmod>{{ page.lastmod | date_to_xmlschema }}</lastmod>
    {% elsif page.date %}
    <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
    {% else %}
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    {% endif %}
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
  </url>
      {% endunless %}
    {% endif %}
  {% endfor %}

</urlset>
